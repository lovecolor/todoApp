import { EmptyLayout } from "../layouts/EmptyLayout"

export const Login = () => {
  // to call API
  // const api = useAppApiClient()
  // api.login({ username: "abc", password: "def" })

  return <EmptyLayout>Login page</EmptyLayout>
}
