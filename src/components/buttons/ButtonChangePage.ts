import styled from "styled-components"
import { ButtonPrimary } from "./ButtonPrimary"

export const ButtonChangPage = styled(ButtonPrimary)`
  position: fixed;
  top: 50%;
  border-radius: 100rem;
  min-width: 56px;
  height: 56px;
  z-index: 100;
`
